import React from 'react';
import { View } from 'react-native';

function Separator() {
  // eslint-disable-next-line react-native/no-inline-styles
  return <View style={{ width: '100%', height: 1, backgroundColor: '#EBEBEB' }} />;
}

export default Separator;
